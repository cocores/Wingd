# Social login (optional — omit either to leave that provider's button hidden).
# Must match the server's GOOGLE_CLIENT_ID / APPLE_CLIENT_ID in server/.env.
VITE_GOOGLE_CLIENT_ID=
VITE_APPLE_CLIENT_ID=
